[build]
  command = "pnpm build:site"
  publish = "dist"
  # Only trigger builds when files in this directory change. See the script for
  # why the pathspec has to be `.` rather than `website/`.
  ignore = "bash netlify-ignore.sh"
