[build]
  command = "pnpm build:site"
  publish = "dist"
  # Only build when files in this directory change. Deploy previews compare against
  # the PR's merge base; production compares against the last built commit.
  # See the script for why the pathspec must be `.` rather than `website/`.
  ignore = "bash netlify-ignore.sh"
